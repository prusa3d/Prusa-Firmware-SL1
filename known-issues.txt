- System: A short power loss triggers emergency A64 board shutdown while MC.
board keeps running (black touchscreen, but power button blinking)
- UI: Back button contain some random nice on the top edge when pressed. (This
  is possibly a HW problem)

